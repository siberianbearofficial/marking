import { DifferentPasswordsValidator } from './different-passwords-validator';

describe('DifferentValuesValidator', () => {
  it('should create an instance', () => {
    expect(new DifferentPasswordsValidator()).toBeTruthy();
  });
});
